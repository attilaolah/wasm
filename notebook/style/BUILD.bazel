load("@io_bazel_rules_sass//:defs.bzl", "sass_binary")

package(default_visibility = ["//visibility:public"])

sass_binary(
    name = "style",
    src = "main.sass",
    deps = [
        "@mdn_yari//:scss",
    ],
)
