load(":clang.bzl", "clang_toolchain")
load(":wasm_toolchain_config.bzl", "wasm_toolchain")

clang_toolchain()

wasm_toolchain(cpu = "wasm32")

wasm_toolchain(cpu = "wasm64")

filegroup(
    name = "builtin_include_directory_paths",
    srcs = ["@local_config_cc//:builtin_include_directory_paths"],
)

filegroup(
    name = "empty",
    srcs = [],
)
