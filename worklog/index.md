# Worklog Index

- 2026-03-24: Bazel revival attempt on Nix; FHS (`steam-run`) bypassed dynamic-linker issues but `bazel query //...` still fails on BUILD API drift (`genrule.exec_tools`). See `worklog/bazel_update.md`.
